const numero = parseInt(prompt("Escriba un numero"));
if(numero % 2 == 0){
    document.write("El numero: " + numero + " " + "es par, o es divisible por 2")
} else {
    document.write("Tu numero es impar, elija otro")
}